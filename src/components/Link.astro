---
interface Props {
  url: string;
  external?: boolean;
}

const { url, external } = Astro.props;

const target = external ? "_blank" : undefined;
---

<a
  href={url}
  target={target}
  class="underline text-cyan-200 hover:text-cyan-400"
>
  <slot />
</a>
